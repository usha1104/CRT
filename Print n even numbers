#include <iostream>
using namespace std;
int main() {
    int num;
    cout<<"Enter the last number=";
    cin>>num;
        for(int i=0;i<=2*num-2;i+=2)
        {
        cout<<"even numbers="<<i<<endl;
        }
    
    return 0;
}
